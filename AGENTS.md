# 專案協作規則

本專案開發 `My Real Second Brain` 開源技能組。所有修改都應維持「第二大腦服務第一大腦」的核心原則。

## 工作原則

- 使用繁體中文撰寫主要文件；必要的指令、套件名與專有名詞保留原文。
- 修改技能前完整閱讀目標 `SKILL.md` 與 `docs/architecture.md`。
- 安裝、更新或修復前，完整閱讀 `INSTALL.md`、`install.manifest.toml` 與 `docs/providers.md`。
- 本地端已由人類消化的知識，具有最高的個人知識優先度。
- Notebook 資料預設為探索性來源，不能未經使用者確認就晉升為核心知識。
- 一人公司設定屬於使用者已確認的長期脈絡；第一次沒有明確任務且設定尚未完成時才引導建立，任何寫入都先預覽並取得確認。
- 創業方向相關問題先讀 `sources/strategy/solopreneur-profile.md` 與既有知識；設定檔不能取代最新證據或使用者當下的明確說法。
- Graphify 是關係與導覽工具，不是事實或第一大腦心得的最高來源。
- Graphify 與 Notebook 是可替換的預設後端；核心流程不得把特定上游指令當成永久介面。
- 來源衝突時並列差異、保留引用，不得靜默覆蓋。
- 使用公開網路資料時提供可直接核對的原始網址，並說明來源支持或挑戰哪一項說法。
- 網路資料與 AI 對話結論預設不寫入知識庫；只有使用者明確要求保存時才依資料角色寫回。
- 不得將 Cookie、Token、Notebook 私人 ID、私人檔案內容或大型來源全文提交到倉庫。
- 不得直接複製沒有相容授權的第三方文件、介面或程式。
- 發布、推送、建立遠端倉庫與刪除資料，必須取得使用者明確確認。

## 實際案例與公開核心的同步

- 如果本機存在 `.local/sync-manifest.toml`，修改共用技能、schema、Provider 契約、安裝或驗證流程前先讀取，辨識 canonical、mirror、runtime consumer 與排除範圍；完成前執行 manifest 指定的一致性檢查。
- 本機 manifest、同步狀態、備份與私人路徑只放在 `.local/`，不得提交。
- 兩邊都在上次同步後修改且內容不同時停止，不自動選邊覆寫。
- `template/sources/` 定義公開版的共用知識庫骨架；實際案例的 `sources/` 必須維持相同的核心目錄與 schema，但可以加入案例專用擴充。
- 實際案例若驗證出技能、工作流程、資料結構、模板、Provider 介面或安裝方式的通用改進，先去識別化與參數化，再回饋本專案。
- 不得從實際案例複製讀書筆記、專案專用規則、Notebook 紀錄、研究資料、Graphify 產物、帳號資訊、私人路徑或未公開內容。
- 公開版若改變共用目錄、schema 或技能契約，必須同時檢查參考案例是否仍相容；不得假設兩邊可以整包覆蓋。
- 「是否回饋公開版」是維護流程，不是一般使用者的執行流程。不得把維護者的本機路徑或同步詢問寫進任何可安裝技能。
- 詳細同步分類與驗證方式見 `docs/development-workflow.md`。

## AI Agent 安裝契約

- 使用者要求安裝時，先檢查環境、既有版本與目標範圍，再展示 `INSTALL.md` 定義的流程與停止點。
- 初始化使用者工作區時，依 manifest 合併 `template/`，只建立缺少項目；遇到既有同名檔案先比較並詢問，不得靜默覆蓋。
- 下載套件、安裝工具、寫入 Agent 技能目錄或開啟登入流程前，先說明變更並取得同意。
- 自有技能清單、安裝範圍、provider 版本與執行順序，以 `install.manifest.toml` 為唯一依據。
- 預設從正式套件來源安裝，不直接 clone 上游 repository，也不把上游原始碼複製進本倉庫。
- 上游技能只安裝在專案範圍，並保持可重新產生；不得混入本專案自行維護的 `skills/`。
- Notebook 登入必須由使用者本人完成。不得讀取、輸出、提交或備份 Cookie、Token 與登入狀態檔。
- 任一步驟無法驗證時停止，保留既有狀態，不宣稱安裝成功。

## 程式與驗證

- 新增程式時優先使用 Python 或 TypeScript，並加入繁體中文註解。
- 變更技能後檢查 YAML frontmatter、路徑、Markdown fence、隱私資訊與第三方來源聲明。
- 範例只能使用虛構資料，不得從使用者實際知識庫複製私人內容。
