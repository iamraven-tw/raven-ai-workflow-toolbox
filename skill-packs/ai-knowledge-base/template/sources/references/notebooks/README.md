# Notebook 本機索引

本目錄只保存路由與同步中繼資料，用來判斷哪個外部集合可能有相關資料，以及上次看到哪些來源；不保存大型來源全文。

- `index.md`：所有已登錄集合的快速路由表。
- `entries/`：每個集合一份詳細來源清單與同步紀錄。

遠端來源清單是權威狀態，本機 entry 是最近一次成功同步快照，`index.md` 是快速摘要。不得保存 Cookie、Token、登入狀態或帳號密碼。
